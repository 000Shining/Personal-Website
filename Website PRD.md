# 产品需求文档（PRD）

## 项目名称：Shiling 个人网站

> **PR/FAQ 一句话描述**：Shiling 个人网站是一个集简历展示、技术博客、作品集于一体的个人品牌官网。访问者无需登录即可浏览公开内容；招聘方通过一键切换身份，可完整查看简历和专业背景——让对的人看到对的内容。

---

## 1. 项目概述

### 1.1 问题陈述

**当前困境**：
- 个人简历以 PDF 形式散落在邮件中，难以被潜在雇主**主动发现**
- 技术文章写在各个博客平台（CSDN、掘金、知乎），内容分散，无法形成**个人品牌积累**
- 项目作品托管在 GitHub，缺乏面向非技术受众（如 HR）的**可视化展示**
- 招聘方需要了解完整背景时，不得不通过多个渠道拼凑信息

**为什么是现在**：在求职市场中，个人网站是最直接的数字名片。一次搭建、长期维护，即可持续产生信任资产。相比于依赖第三方平台，自建网站给予 100% 的内容控制权和品牌表达自由。

### 1.2 产品目标与成功标准

| 目标 | 成功标准 |
|------|----------|
| 实现 4 个核心页面 | 首页、About、Blog、Portfolio 全部可访问，无 404 |
| 角色区隔 | Visitor 无法访问 About 页；HR 可完整浏览 |
| 国内可访问 | 从国内网络打开网站首屏 < 3 秒 |
| 零成本部署上线 | 不产生域名和托管费用，月成本 = 0 |
| 可维护性 | 新增一篇文章 ≤ 10 分钟（创建 .md 文件 + 更新索引 JSON） |

### 1.3 目标用户

| 角色 | 典型场景 | 核心诉求 |
|------|----------|----------|
| **访客（Visitor）** | 通过搜索引擎/社交链接进入，想了解这个人是谁、写过什么、做过什么 | 快速浏览、判断是否值得关注 |
| **HR/招聘方** | 收到简历后被引导至此，想全面评估技术能力和项目经验 | 查看完整履历、验证项目真实性、下载简历 PDF |

### 1.4 技术约束

- 纯前端静态网站（HTML/CSS/JS），无需数据库
- 托管于 **GitHub Pages**（`*.github.io` 国内可访问，支持自定义域名）
- 页面需具备**响应式设计**，适配 PC、平板、手机端

### 1.5 明确不做什么（Out of Scope）

- 不做用户注册/登录系统
- 不做评论/留言功能
- 不做后台管理面板（CMS 后期可选接入，不在 V1 范围）
- 不做多语言（仅中文+英文标题即可）
- 不接第三方统计（如 Google Analytics，保持轻量）

---

## 2. 功能需求

### 2.1 首页（Home）

**用户故事**：作为一个首次访问者，我希望一眼就知道这个网站是什么，并快速找到适合我的浏览方式。

**功能描述**：网站的入口页面，简洁明了地传达网站身份，并提供角色选择入口。

**详细需求**：
- 页面中央显著展示欢迎语：**"Welcome to Shiling's Personal Website!"**
- 欢迎语下方放置两个醒目的按钮：
  - **Visitor（访客）**—— 以访客模式浏览公开内容
  - **HR（招聘方）**—— 以 HR 模式浏览，允许访问简历页面
- 1 秒内完成选择即可跳转，无需输入任何信息

**交互逻辑**：
1. 点击任一按钮 → 记录角色状态到 `localStorage`（`userRole: "visitor" | "hr"`）
2. 自动跳转到 Blog 页面（作为默认内容入口）
3. 全局导航根据角色状态控制 About 入口的可见性

**成功标准**：首次访问者从打开首页到进入内容页 ≤ 3 秒

---

### 2.2 个人简历页面（About / Resume）

**用户故事**：作为一个 HR，我希望在一个页面上看到候选人的完整履历，结构清晰、信息完整，最好能直接下载 PDF 版本。

**功能描述**：仅 HR 角色可见，展示完整且结构化的个人履历信息，风格参考 LinkedIn 的信息架构。

**详细需求**：

页面顶部展示：
- 个人头像 / 姓名
- 一句话简介（如"前端开发工程师 · 3 年经验"）

内容模块：
1. **工作经历**（时间线风格）：公司名称、职位、时间段、工作描述
2. **项目经历**（时间线风格）：项目名称、指导老师（可选）、时间段、项目描述
3. **教育背景**：学校、学位、时间段
4. **专业技能**：技术栈标签组（如 JavaScript、React、CSS、Python 等）
5. **语言 / 证书 / 获奖**（可选）

底部操作区：
- "下载简历 PDF"按钮
- "联系我"按钮

**权限控制**：
- 非 HR 角色访问 → 自动重定向回首页，显示简短提示

**成功标准**：HR 用户能在 30 秒内完成履历浏览并定位到关键信息（工作经历、技能栈）

---

### 2.3 个人文章页面（Blog）

**用户故事**：作为一个读者，我希望看到清晰排版的文章，并且能通过目录快速跳转到我感兴趣的部分。

**功能描述**：展示 Shiling 发布的个人技术/生活文章。文章以 Markdown 格式编写，在客户端实时渲染为富文本，并提供层级目录（TOC）导航。

#### 2.3.1 列表页

- **卡片布局**：每张卡片包含文章标题、发布时间、摘要、标签
- **排序**：按时间倒序排列
- **交互**：点击卡片或"阅读更多"进入详情页
- **分页**（可选）：文章超过 10 篇时支持"加载更多"

#### 2.3.2 详情页

- 显示文章完整标题、正文内容、元数据（发布时间、分类标签）
- **Markdown 渲染**：文章正文以 Markdown 格式存储，通过 `marked.js` 在客户端实时渲染为 HTML
- **层级目录（TOC）**：
  - 自动解析 Markdown 中的标题层级（H2、H3、H4）
  - 在页面左侧侧边栏（PC）或顶部可折叠面板（移动端）生成可点击的目录导航
  - 目录项缩进展示完整层级结构
  - 点击目录项平滑滚动到对应段落
  - 滚动阅读时当前目录项自动高亮（Intersection Observer）

#### 2.3.3 内容管理方式

- 文章以 **Markdown 文件**（`.md`）形式存储在 `/posts/` 目录下
- 文章元数据通过 **frontmatter**（YAML 头）定义：标题、日期、摘要、标签
- 通过 `/data/posts.json` 索引文件汇总所有文章信息
- **后期可选**：接入 Decap CMS / Sveltia CMS 实现在线编辑

**Markdown 文章格式示例**：

```markdown
---
id: 1
title: "我的前端学习之路"
date: "2026-05-01"
summary: "回顾从零基础到独立完成项目的历程..."
tags: ["前端", "感悟"]
---

## 起步阶段

最初接触前端是从 HTML 和 CSS 开始...

### 第一个项目

做了一个简单的个人主页...

## 进阶学习

后来开始学习 React 框架...
```

#### 2.3.4 TOC 层级目录规格

- **位置**：详情页左侧侧边栏（PC）/ 顶部可折叠面板（移动端）
- **层级**：H2 → H3 → H4 缩进嵌套
- **交互**：当前阅读位置自动高亮（主色 `#345b80`）；点击平滑滚动
- **定位**：`position: sticky`，随页面滚动保持可见

**成功标准**：一篇 3000 字文章从加载到可阅读 ≤ 2 秒；TOC 点击跳转延迟 ≤ 200ms

---

### 2.4 个人作品展示页面（Portfolio）

**用户故事**：作为一个想了解我项目经验的人（无论是技术同行还是 HR），我希望通过图文并茂的方式看到我做过什么项目、用了什么技术、最终效果如何。

**功能描述**：展示个人项目或设计作品，以卡片网格布局呈现，点击卡片显示项目详情。

#### 2.4.1 列表页

- **卡片视图**：每个卡片包含项目封面图（可选）、项目名称、简短描述、技术标签
- **点击**：打开项目详情页

#### 2.4.2 详情页

- 项目完整介绍、过程截图或 Demo 链接
- 技术栈标签、上线链接、GitHub 仓库地址

#### 2.4.3 布局

- **Grid 栅格布局**，响应式适配：
  - PC 端：3 ~ 4 列
  - 平板：2 列
  - 手机：1 列
- **可选**：分类筛选标签组（如"Web 开发 / 移动端 / 视觉设计"）

**成功标准**：作品卡片在 PC 端一屏至少展示 6 个项目，无需滚动即可概览

---

### 2.5 全局导航与通用要求

**用户故事**：作为浏览者，我需要在任何页面都能方便地切换到其他内容区域，并且知道我当前在哪里。

**功能描述**：每个页面顶部均有全局导航栏，方便用户在不同页面之间跳转。

**详细需求**：

导航栏包含链接：
| 链接 | 可见性 |
|------|--------|
| Home | 始终可见 |
| Blog | 始终可见 |
| Portfolio | 始终可见 |
| About / Resume | 仅 HR 角色可见 |

角色状态管理：
- 角色信息存储在 `localStorage`，各页面加载时读取
- 无存储记录时默认为访客模式
- 导航栏或页脚提供"切换身份"入口，可返回首页重新选择

页脚：
- 版权信息
- 社交链接（GitHub、LinkedIn 等）

**页面通用规范**：
- HTML/CSS/JS 遵循语义化标签和现代前端最佳实践
- 整体 UI 干净、专业，保留个人风格装饰元素

---

## 3. 非功能性需求

### 3.1 性能
- 首屏加载时间 < 2 秒（3G 网络环境）
- 图片懒加载（`loading="lazy"`）
- CSS/JS 文件合并压缩，总体积 < 100KB（不含图片）

### 3.2 可访问性
- 语义化 HTML 标签（`header`、`nav`、`main`、`article`、`footer`）
- 色彩对比度满足 WCAG AA 标准
- 链接和按钮具备可识别的 hover/focus 状态

### 3.3 兼容性
- 支持 Chrome、Firefox、Safari、Edge 最新两个大版本
- 移动端浏览器适配

### 3.4 可维护性
- 全局 CSS 变量统一管理颜色、间距、阴影
- 文章和项目数据与页面代码分离
- 新增文章：创建 .md 文件 + 更新 posts.json 索引，≤ 10 分钟

---

## 4. UI 设计风格与配色方案

基于 `color.webp` 提取的配色，整体风格定位为**"沉稳专业 + 温暖点缀"**。

### 4.1 色彩系统

| 用途 | 色值 | 说明 |
|------|------|------|
| **主色（Primary）** | `#345b80` | 深钢蓝 —— 导航栏、标题、链接、按钮、TOC 高亮 |
| **强调色（Accent）** | `#faaf92` | 三文鱼暖粉 —— 关键按钮、hover 效果、标签、强调元素 |
| **正文色（Text）** | `#27465b` | 深海军蓝 —— 正文段落文字 |
| **背景色（Background）** | `#ffffff` | 白色 —— 主背景 |
| **次级背景（Surface）** | `#f5f7fa` | 浅蓝灰 —— 卡片背景、代码块、区域分隔 |
| **边框色（Border）** | `#e0e6ed` | 浅灰蓝 —— 分割线、卡片边框 |
| **浅主色（Primary Light）** | `#e8eef4` | 极浅蓝 —— hover 背景、选中状态背景 |

### 4.2 设计风格

**整体定位**：专业编辑风 + 现代极简主义

- **卡片**：轻微圆角（8px），浅色背景 `#f5f7fa`，hover 时上浮 4px + 柔和阴影
- **导航栏**：主色 `#345b80` 背景，白色文字，固定顶部（`sticky`）
- **排版**：
  - 正文行高 1.6，基准字号 16px
  - 标题使用主色 `#345b80`
  - 链接使用主色，hover 切换为强调色 `#faaf92`
- **按钮**：
  - 主按钮：主色 `#345b80` 背景 + 白色文字
  - 强调按钮：强调色 `#faaf92` 背景 + 深色文字
- **动效**：
  - hover 时卡片上浮 + 阴影加深（`transform: translateY(-4px)`）
  - 按钮 hover 使用浅色填充过渡
  - 页面切换平滑滚动（`scroll-behavior: smooth`）

### 4.3 CSS 变量定义

```css
:root {
  --color-primary: #345b80;
  --color-primary-light: #e8eef4;
  --color-accent: #faaf92;
  --color-text: #27465b;
  --color-bg: #ffffff;
  --color-surface: #f5f7fa;
  --color-border: #e0e6ed;
  --font-size-base: 16px;
  --line-height-base: 1.6;
  --border-radius: 8px;
  --shadow-sm: 0 1px 3px rgba(39, 70, 91, 0.08);
  --shadow-md: 0 4px 12px rgba(39, 70, 91, 0.12);
  --transition: 0.2s ease;
}
```

### 4.4 设计原则

- 保证可读性，正文行高 1.6，基准字号 16px
- 导航栏和主要组件使用 Flex / Grid 实现布局
- 全局使用 CSS 变量控制颜色、间距、阴影，便于维护
- 所有卡片均采用 Grid 网格布局实现响应式排列
- 色彩对比度满足 WCAG AA 标准

---

## 5. 前端技术选型与架构

### 5.1 核心技术

| 技术 | 用途 |
|------|------|
| **HTML5** | 语义化标签（`header`、`nav`、`main`、`article`、`footer`），SEO 友好 |
| **CSS3** | Flex + Grid 布局，CSS 变量，响应式媒体查询 |
| **JavaScript (ES6+)** | 角色切换、本地存储、数据加载、权限控制 |
| **[marked.js](https://github.com/markedjs/marked)** | Markdown 解析渲染（Blog 详情页），6KB gzipped |

### 5.2 整体架构

- **多页面应用（MPA）**：每个页面是独立的 `.html` 文件，通过导航栏链接跳转
- **共享通用布局**：CSS 和公共 JS 函数抽离至外部文件，跨页面复用
- **数据层**：
  - Blog 文章以 `.md` 文件存储在 `/posts/` 目录
  - 文章索引和作品数据以 JSON 存储在 `/data/` 目录
  - 页面加载时通过 `fetch()` 获取并动态渲染
- **路由**：Blog 详情页通过 URL 参数 `?id=1` 区分文章，Portfolio 同理
- **角色状态**：`localStorage` 存储，各页面加载时读取

### 5.3 页面目录结构

```
个人网站/
├── index.html               # 首页（角色选择入口）
├── blog.html                # 文章列表页
├── blog-detail.html         # 文章详情页（含 TOC）
├── portfolio.html           # 作品列表页
├── portfolio-detail.html    # 作品详情页
├── about.html               # 简历页面（仅 HR 可见）
├── css/
│   └── style.css            # 全局样式 + CSS 变量
├── js/
│   ├── main.js              # 共用函数（导航高亮、角色检查、footer 注入）
│   ├── role.js              # 角色选择与本地存储逻辑
│   ├── blog.js              # 博客列表加载与渲染
│   ├── blog-detail.js       # 文章详情：Markdown 渲染 + TOC 生成
│   └── portfolio.js         # 作品数据加载与渲染
├── data/
│   ├── posts.json           # 文章索引
│   └── projects.json        # 作品数据
├── posts/                   # Markdown 文章文件
│   └── 2026-05-01-my-frontend-journey.md
├── images/                  # 静态图片资源
└── resume.pdf               # 可下载的简历 PDF
```

---

## 6. 部署方案：GitHub Pages

### 6.1 为什么选择 GitHub Pages

- **零成本**：免费托管，月带宽 100GB，仓库 ≤ 1GB
- **国内可达**：`*.github.io` 在国内大部分地区可正常访问
- **零运维**：`git push` 即自动部署，无需配置 CI/CD
- **天然 SSL**：自动提供 HTTPS 证书
- **版本管理**：网站代码天然受 Git 版本控制

### 6.2 部署步骤

1. 创建 GitHub 仓库（推荐命名 `shiling.github.io`）
2. 推送静态文件到仓库
3. Settings → Pages → Source: `Deploy from a branch` → 选择 `main` 分支，根目录 `/`
4. 访问 `https://shiling.github.io` 即完成

### 6.3 自定义域名（可选）

1. Settings → Pages → Custom domain → 填入个人域名
2. DNS 添加 CNAME 记录指向 `shiling.github.io`
3. GitHub 自动申请并续期 Let's Encrypt SSL

---

## 7. 数据模型说明

### 7.1 文章索引（posts.json）

```json
[
  {
    "id": 1,
    "title": "我的前端学习之路",
    "date": "2026-05-01",
    "summary": "回顾从零基础到独立完成项目的历程...",
    "tags": ["前端", "感悟"],
    "file": "posts/2026-05-01-my-frontend-journey.md"
  }
]
```

### 7.2 Markdown 文章文件（.md）

```markdown
---
id: 1
title: "我的前端学习之路"
date: "2026-05-01"
summary: "回顾从零基础到独立完成项目的历程..."
tags: ["前端", "感悟"]
---

## 起步阶段

最初接触前端是从 HTML 和 CSS 开始...

### 第一个项目

做了一个简单的个人主页，学会了布局和样式...

### 遇到的问题

跨浏览器兼容性是一个挑战...

## 进阶学习

后来开始学习 React 框架，理解了组件化开发的思想...
```

### 7.3 作品数据（projects.json）

```json
[
  {
    "id": 1,
    "name": "个人博客网站",
    "description": "基于原生 HTML/CSS/JS 搭建的个人展示网站",
    "image": "images/project1.jpg",
    "techStack": ["HTML5", "CSS3", "JavaScript"],
    "detail": "<p>项目详情...</p>",
    "liveLink": "https://example.com",
    "githubLink": "https://github.com/shiling/personal-website"
  }
]
```

---

## 8. 页面交互流程

### 8.1 角色选择与权限控制

```
用户首次访问 → index.html
                  │
     ┌────────────┼────────────┐
     ▼                         ▼
  [Visitor]                 [HR]
     │                         │
     ▼                         ▼
localStorage              localStorage
role = 'visitor'          role = 'hr'
     │                         │
     ▼                         ▼
跳转 blog.html            跳转 blog.html
About 入口隐藏            About 入口可见
```

### 8.2 内容浏览流程

```
导航栏切换 → Blog 列表页
              │
              加载 data/posts.json
              │
              动态渲染卡片列表
              │
              点击"阅读更多"
              │
              ▼
         详情页 (blog-detail.html?id=1)
              │
              ├── fetch() 获取 .md 文件
              ├── 解析 frontmatter 元数据
              ├── marked.js 渲染 Markdown → HTML
              └── 生成 TOC 层级目录（侧边栏 sticky 导航）
```

### 8.3 退出 / 重置角色

- 导航栏或页脚提供"切换身份"入口
- 点击 → 清除 `localStorage` 角色 → 跳转回 `index.html`

---

## 9. 关键功能实现要点

### 9.1 Markdown 渲染 + TOC 实现方案

**技术选型**：
- 渲染：`marked.js`（轻量，6KB gzipped）
- 代码高亮（可选）：`highlight.js` 或 `Prism.js`

**TOC 生成流程**：
1. `marked.js` 渲染 Markdown 为 HTML
2. 遍历渲染后的 DOM，提取所有 `h2`、`h3`、`h4` 元素
3. 为每个标题生成唯一 `id`（用于锚点跳转）
4. 构建嵌套 TOC 树结构（`h2 > h3 > h4`）
5. 渲染 TOC 到侧边栏，绑定点击事件（平滑滚动）
6. `Intersection Observer` 监听标题可见性 → 高亮当前 TOC 项

### 9.2 角色权限控制

```javascript
// role.js
function getRole() {
  return localStorage.getItem('userRole') || 'visitor';
}

function requireRole(requiredRole) {
  if (getRole() !== requiredRole) {
    window.location.href = 'index.html';
  }
}

// about.html 中调用
requireRole('hr');
```

---

## 10. 总结

本 PRD 定义了一个面向访客和招聘方的个人展示网站。核心价值主张：**让对的人看到对的内容——访客浏览公开作品和文章，HR 完整查看履历和专业背景。**

| 维度 | 决策 |
|------|------|
| **内容展示** | Blog、Portfolio、About/Resume 四个核心页面 |
| **角色区隔** | 访客/HR 双角色，基于 `localStorage` 的轻量权限控制 |
| **Blog 系统** | Markdown + frontmatter 编写，marked.js 渲染，自动 TOC 层级导航 |
| **配色方案** | 基于 `color.webp` 的"深钢蓝 #345b80 + 三文鱼粉 #faaf92"系统 |
| **部署方案** | GitHub Pages 免费托管，`git push` 即部署 |
| **技术栈** | 原生 HTML/CSS/JS + marked.js，零后端依赖 |

---

*本 PRD 参考了 11 位产品领袖的 PRD 方法论（via Lenny's writing-prds skill），在问题驱动、成功标准定义、Out of Scope 界定等方面进行了增强。*
